# AI 找数 v3 演示脚本：样例问题 × 覆盖场景

## 一、找数意图（业务描述 → 推荐资产）

| 输入 | 路由 | 演示要点 |
|---|---|---|
| 各渠道 GMV 表现 | 找资产 | 核心找数场景：3 张卡（报表 > 看板 > 表），排序体现"报表优先"；每张卡按类型给不同用数按钮（预览报表 / 预览看板 / 打开表） |
| 配送时效看板 | 找资产 | 看板类资产优先推荐；点"预览看板"演示嵌入预览 |
| 用户留存相关报表 | 找资产 + 权限示例 | 报表卡是"申请权限"状态 → 点击变"申请中"；推荐理由里提示"报表权限未下来之前可先用表" |
| 用户下单后的退款情况（最近记录） | 找资产 | 口语化业务描述也能召回；从右侧空态"最近找数记录"点入 |

## 二、查数意图（指标 + 分析/时间词 → 直接出数）

| 输入 | 路由 | 演示要点 |
|---|---|---|
| 昨天 GMV 是多少 | 查数-指标值（I001） | 左侧摘要气泡 + 右侧指标卡；**来源与口径默认展开**（支付口径、不含退款、T+1） |
| 昨天 GMV 按城市分布 | 查数-拆解（I006） | 城市排名表格；来源面板可点"用来源表继续查 →"跳进表工作区 |
| 最近 30 天 GMV 趋势 | 查数-趋势（I002） | 折线图 + 汇总卡 |
| 本周 GMV 和上周对比 | 查数-对比（I004） | 逐日对比表，今天标注"统计中（未完整）"体现口径严谨 |
| GMV 最高的城市 TOP5 | 查数-排名（I003） | 🥇🥈🥉 榜单 + 占全国比例；来源标注 ORDER BY + LIMIT |
| 各渠道 GMV 占比 | 查数-占比（I005） | 渠道份额条（58.4% / 31.2% / 10.4%）+ 占比 pp 环比 |
| 昨天的订单明细 | 明细（I007） | 不出聚合数，直达 dwd_trade_order_detail 表信息页（结构 + 说明）；说"退款明细"则打开退款明细表；查询从页内去即席查询 |
| GMV 的口径是什么 | 定义（I008，不走 SQL） | 直接回指标注册表里的口径卡片，不执行查询；附"直接看数 / 找相关资产"引导；查完 GMV 后问"口径是什么"演示上下文补位 |
| 预测下个月 GMV | 预测（暂不支持） | 诚实告知在规划中，引导先看趋势/对比 |
| 昨天 GMV 为什么涨了 | 洞察（I009） | 四段式：💡核心结论 → 📊增量归因（渠道贡献度条）→ 🔍异常检测（1 条预期内 + 1 条需关注）→ 👉继续分析；口径面板标注归因方法（贡献度分解、±2σ 异常阈值、结论仅供参考）。变体："分析一下 GMV""GMV 有什么异常"；查完 GMV 后问"为什么涨了"演示上下文 |

## 三、上下文（多轮追问）

| 输入 | 前置 | 演示要点 |
|---|---|---|
| 趋势呢 / 按城市呢 | 先问"昨天 GMV 是多少" | 短追问不带主语 → 从上下文补指标，回复标注"已沿用上下文"；输入框上方有上下文条（可 ✕ 清除） |
| 点击历史摘要气泡 | 多次查数后 | 每条查数记录可回看当时的图表，不会被后面的覆盖 |

## 四、歧义与诚实降级（体现"系统听懂了/听不懂会说"）

| 输入 | 路由 | 演示要点 |
|---|---|---|
| 昨天销售额是多少 | 指标确认 | "销售额"命中 GMV（支付口径）/ 营收（确收口径）两个候选，各带口径摘要和"是否已接入查数"标注；选 GMV 直接出数，选营收走诚实降级。进阶演示："本周销售额和上周对比"→ 确认后直接出对比，原句槽位保留 |
| GMV（只输入指标名） | 默认出数 | 不追问，直接返回昨日 GMV 指标卡 |
| 退款率趋势 | 查数意图 → 降级找资产 | 回显识别到的槽位："已识别你想查「退款率」的趋势，但该指标还没接入直接查数"；对应真实系统的指标注册机制 |
| 帮我找点数据 | 模糊追问 | 给 4 个业务方向选项，只追问一次 |
| user_profile_full | 映射未命中 | 诚实说明"血缘里没找到"，不硬塞结果 |
| 随便乱输入 | 兜底 | 诚实"没找到"+ 方向引导，不再默认塞 GMV |

## 五、表名映射（技术用户场景）

| 输入 | 路由 | 演示要点 |
|---|---|---|
| order 表在数仓里是哪张 | 血缘映射 | ODS / DWD / DWS 三张下游表，各带选用理由（核对数据 / 明细分析 / 看趋势） |

## 六、用数闭环（找到之后，演示重头戏）

| 操作 | 演示要点 |
|---|---|
| 报表卡 → 预览报表 | 右侧嵌入预览：筛选条 + KPI + 趋势图 + 指标口径；"打开原报表"提示携带筛选条件跳 BI |
| 表卡 → 打开表 | 表信息页：元信息 + 业务说明 + **表结构默认展开**（字段/类型/业务含义，换退款表演示 schema 跟着变） |
| 表信息页 → 去即席查询工作台 | 提示携带表名与字段上下文跳转；SQL 能力统一在即席查询模块 |
| 查数结果 → 打开来源表 | 从数字回溯到来源表信息，查证链路 |
| 任意卡片 → 详情 | **统一详情抽屉**（与平台 DetailPage 一致）：双层头部、字段安全等级（user_id 为 S3，样例脱敏 ******）、分区、血缘链路、DDL 变更；"打开这张表 →"回表信息页 |

## 建议演示动线（10 分钟）

1. 各渠道 GMV 表现 → 预览报表 → 看口径（找数→用数，报表路径）
2. 回到卡片 → 表卡"打开表" → 表结构与业务说明 → 去即席查询（表路径，SQL 能力归工作台）
3. 昨天 GMV 是多少 → 按城市呢（查数 + 上下文）→ 昨天 GMV 为什么涨了（洞察归因）→ 打开来源表
4. 昨天销售额是多少（指标确认）→ GMV 的口径是什么（定义不走 SQL）→ 退款率趋势（诚实降级）→ 预测（诚实不支持）
5. order 表映射 → 详情抽屉（技术用户 + 统一详情 + 脱敏）
