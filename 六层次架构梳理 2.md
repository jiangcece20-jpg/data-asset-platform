# ChatBI 平台 · 六层次架构梳理（文字稿 / 待校对）

> 依据《功能模块.xlsx》整理。原表共 **9 个模块 / 约 60 个二级功能 / 227 个三级功能**。
> 本稿将 9 个模块收敛映射为 **6 层**，自底向上排列。

## 收敛思路（9 模块 → 6 层）

| 原表模块 | 归入层 |
|---|---|
| 部署、安全与运维 + 集成开放 | 第 1 层 平台底座层 |
| 数据与语义层 + 数据工程与平台 AI | 第 2 层 数据与语义层 |
| 知识管理与准确性 | 第 3 层 知识与准确性层 |
| Agent 与扩展 | 第 4 层 智能内核层（Agent 引擎） |
| 高级分析 | 第 5 层 分析与洞察层 |
| 对话问数 + 可视化与仪表盘 | 第 6 层 交互与体验层 |

主要合并动作：① 集成开放并入平台底座；② Fabric 特色的"数据工程与平台 AI"作为数据层的扩展能力；③ 对话入口与可视化合为一个面向用户的交互层。

---

## 第 6 层 · 交互与体验层（最上层，直接面向用户）

**定位**：用户与平台对话、看结果、用看板的所有入口。

**对话问数**：自然语言问数、NL2SQL / NL2Query、多轮对话、意图识别路由、问题改写、对话资产、Agent 问数、多语言、跨数据源查询

**可视化与仪表盘**：一句话建看板、图表能力（常规/高级/自定义）、仪表盘使用（布局/筛选/下钻联动）、定时刷新、条件格式、叙事视觉、嵌入可视化

---

## 第 5 层 · 分析与洞察层

**定位**：从"查数"走向"分析助手"，主动给结论。

**高级分析**：归因分析、预测分析、异常检测、报告生成、AI 洞察、KPI 监控、实时智能、AI Functions、自修正（SQL 失败自动重试）

---

## 第 4 层 · 智能内核层（Agent 引擎）

**定位**：调度、推理、编排与记忆的"大脑"，串联上下各层能力。

**Agent 与扩展**：工作流编排、ReAct 闭环（执行-观察-反思）、Skill / 插件、MCP 集成、A2A 协议、记忆管理、智能学习、定时任务、模型接入（含私有大模型）

---

## 第 3 层 · 知识与准确性层

**定位**：所有"让 AI 答得准"的知识与治理机制，是差异化核心。

**知识管理与准确性**：知识库 / RAG、术语词库、知识审核、检索调试、AI Data Schema（字段暴露控制）、AI Instructions、Verified Answers（标准答案库）、示例查询（Few-shot）、知识同步调优、分析思路模板、Coaching、评测体系

---

## 第 2 层 · 数据与语义层

**定位**：指标、口径、模型、向量与图谱构成的语义地基。

**数据与语义层**：指标模型、口径管理、多表建模、数据转换（dbt/血缘/发布）、时间层次、缓慢渐变维、数据向量化、知识图谱、文件数据、数据资产市场

**数据工程与平台 AI（扩展，Fabric 特色）**：Notebook 辅助、故障诊断、Pipeline 生成、性能优化

---

## 第 1 层 · 平台底座层（最底层）

**定位**：部署形态、安全合规、运维与对外集成能力，支撑上层全部运行。

**部署、安全与运维**：私有化部署（单节点/集群/K8s）、SaaS、沙盒环境、身份认证（SSO/MFA/组织同步）、权限控制（功能/数据/行级）、数据保护（脱敏/下载管控/水印）、审计日志、多租户、运维能力

**集成开放**：REST API、Webhook、IFRAME 嵌入、SDK、企业 IM（飞书/钉钉企微/Slack Teams）、版本控制（Git/回滚）、部署管道

---

## 待你确认的几点

1. **层的顺序与命名**是否认可（尤其"智能内核层"放第 4 层、贯穿上下）。
2. **集成开放**并入底座是否合适 —— 也可以改成右侧"贯穿式竖条"（跨层能力），而非独立一层。
3. **数据工程与平台 AI** 是否保留（Fabric 独有、非近期重点），或直接从图上省略。
4. 是否需要在图上标注 **当前状态 / 竞品对比**（白泽 vs 恒索/阿里云/腾讯云/PBI/Fabric/ThoughtSpot），还是只画能力架构本身。
